package main.java.com.example.pokedex.utilities;

/**
 * Interface for generating HTML-formatted data.
 */
public interface HTMLGenerator {
    /**
     * Generates an HTML-formatted string.
     *
     * @return The HTML-formatted string.
     */
    String generateHTML();
}
